---

const steps = [
  {
    title: 'Sube tu PDF/Excel',
    description: 'Subí el archivo PDF o Excel y dejá que AuditAi se encargue del resto en segundos.'
  },
  {
    title: 'La IA analiza tus finanzas',
    description: 'Nuestra inteligencia artificial procesa ingresos, gastos e impuestos, detectando errores e inconsistencias automáticamente.'
  },
  {
    title: 'Analiza tu reporte',
    description: 'Descargá un informe claro y estructurado con resultados listos para tomar decisiones contables inteligentes.'
  }
]
;
---
<div class="flex flex-col md:flex-row md:gap-2 md:w-[60%] w:full justify-center items-center">
    {steps.map((step, i) => (
        <div class="rounded-lg bg-gradient-to-r from-blue-900 via-indigo-700 to-teal-600 p-0.5  w-full md:w-[24rem] h-auto mb-4 md:mb-0">
            <div class="bg-neutral-950 text-slate-300 rounded-lg w-full h-full px-6 py-6 flex flex-col">
              <span class="bg-zinc-900 hover:bg-slate-800 px-2 py-1 rounded-md text-slate-400 font-semibold w-32 text-center md:w-16">Paso {i + 1}</span>
              <span class="text-white text-2xl mt-3">{step.title}</span>
              <span class="text-slate-300 text-[0.8rem] mt-3">{step.description}</span>
            </div>
        </div>
    ))}
</div>
