---
import Layout from '../../layouts/Layout.astro'
import { funciones } from '../../data/funciones.ts'
import Example from '../../components/functions/Example.astro'

export async function getStaticPaths () {
  return funciones.map((f) => ({
    params: { slug: f.slug },
    props: f
  }))
}

const { title, description, content, icon, bullets, example } = Astro.props;
---

<Layout title={`${title} | AuditAi` } fill1="#19707a" fill2="#4e197a">
    <main class="text-white md:px-40 md:py-6 relative">
        <!-- Decorative background -->
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div class="absolute top-20 left-10 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl"></div>
            <div class="absolute bottom-40 right-10 w-96 h-96 bg-teal-500/10 rounded-full blur-3xl"></div>
        </div>

        <section class="relative mt-12 w-full flex flex-col items-center text-center px-4 md:px-0">
            <!-- Breadcrumb -->
            <nav class="w-full max-w-4xl mb-8 flex items-center gap-2 text-sm text-slate-400">
                <a href="/" class="hover:text-white transition-colors">Inicio</a>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                </svg>
                <a href="/funciones" class="hover:text-white transition-colors">Funciones</a>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                </svg>
                <span class="text-white font-medium">{title}</span>
            </nav>

            <!-- Icon with gradient background -->
            <div class="relative mb-8 group">
                <div class="absolute inset-0 bg-gradient-to-r from-blue-600 to-teal-400 rounded-2xl blur-xl opacity-30 group-hover:opacity-50 transition-opacity duration-300"></div>
                <div class="relative w-24 h-24 bg-gradient-to-br from-blue-600 to-indigo-600 rounded-2xl flex items-center justify-center shadow-2xl group-hover:scale-110 transition-transform duration-300">
                    <span
                        class="text-5xl md:text-6xl drop-shadow-lg"
                        transition:name={`${title}-${icon}`}
                    >
                        {icon}
                    </span>
                </div>
            </div>

            <!-- Title with accent -->
            <h1
                class="text-4xl md:text-6xl font-bold text-white tracking-tight mb-4 leading-tight"
                transition:name={title}
            >
                {title}
            </h1>

            <!-- Underline decoration -->
            <div class="w-24 h-1 bg-gradient-to-r from-blue-600 via-indigo-600 to-teal-400 rounded-full mb-6"></div>

            <!-- Description with better styling -->
            <p class="text-slate-300 mt-4 max-w-3xl text-lg md:text-xl leading-relaxed">
                {description}
            </p>

            <!-- Main content card -->
            <div class="mt-12 max-w-4xl w-full">
                <div class="bg-zinc-900/60 backdrop-blur-sm border border-slate-700/50 rounded-2xl p-8 md:p-12 text-left shadow-xl">
                    <!-- Content -->
                    <div class="text-slate-300 leading-relaxed space-y-6">
                        <p class="text-base md:text-lg">{content}</p>

                        {bullets && bullets.length > 0 && (
                            <div class="mt-8">
                                <h3 class="text-xl font-bold text-white mb-4 flex items-center gap-2">
                                    <svg class="w-6 h-6 text-teal-400" fill="currentColor" viewBox="0 0 20 20">
                                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                                    </svg>
                                    Características principales
                                </h3>
                                <ul class="space-y-4">
                                    {bullets.map((b) => (
                                        <li class="flex items-start gap-3 group">
                                            <div class="flex-shrink-0 w-6 h-6 rounded-full bg-gradient-to-br from-blue-600 to-teal-400 flex items-center justify-center mt-0.5 group-hover:scale-110 transition-transform duration-200">
                                                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                                                </svg>
                                            </div>
                                            <span class="text-slate-300 group-hover:text-white transition-colors flex-1">{b}</span>
                                        </li>
                                    ))}
                                </ul>
                            </div>
                        )}
                    </div>

                    {example && (
                        <div class="mt-10">
                            <div class="border-t border-slate-700/50 pt-8">
                                <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-2">
                                    <svg class="w-7 h-7 text-teal-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"/>
                                    </svg>
                                    Ejemplo práctico
                                </h3>
                                <Example
                                    exampleDescription={example.exampleDescription}
                                    images={example.images}
                                    files={example.files}
                                />
                            </div>
                        </div>
                    )}
                </div>
            </div>

            <!-- Benefits section -->
            <div class="mt-12 max-w-4xl w-full">
                <div class="grid md:grid-cols-3 gap-6">
                    <div class="bg-zinc-900/60 backdrop-blur-sm border border-slate-700/50 rounded-xl p-6 text-center hover:border-blue-500/50 transition-all duration-300 hover:-translate-y-1">
                        <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center mx-auto mb-4">
                            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                            </svg>
                        </div>
                        <h4 class="text-lg font-bold text-white mb-2">Ahorro de tiempo</h4>
                        <p class="text-slate-400 text-sm">Automatizá procesos que antes tomaban horas</p>
                    </div>

                    <div class="bg-zinc-900/60 backdrop-blur-sm border border-slate-700/50 rounded-xl p-6 text-center hover:border-teal-500/50 transition-all duration-300 hover:-translate-y-1">
                        <div class="w-12 h-12 bg-gradient-to-br from-teal-500 to-emerald-600 rounded-lg flex items-center justify-center mx-auto mb-4">
                            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                            </svg>
                        </div>
                        <h4 class="text-lg font-bold text-white mb-2">Mayor precisión</h4>
                        <p class="text-slate-400 text-sm">Reducí errores humanos al mínimo</p>
                    </div>

                    <div class="bg-zinc-900/60 backdrop-blur-sm border border-slate-700/50 rounded-xl p-6 text-center hover:border-indigo-500/50 transition-all duration-300 hover:-translate-y-1">
                        <div class="w-12 h-12 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-lg flex items-center justify-center mx-auto mb-4">
                            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z"/>
                            </svg>
                        </div>
                        <h4 class="text-lg font-bold text-white mb-2">Escalabilidad</h4>
                        <p class="text-slate-400 text-sm">Crecé sin límites operativos</p>
                    </div>
                </div>
            </div>

            <!-- Enhanced CTA Section -->
            <div class="mt-16 mb-12 w-full max-w-4xl">
                <div class="relative group">
                    <div class="absolute -inset-1 bg-gradient-to-r from-blue-600 via-indigo-600 to-teal-400 rounded-2xl blur-lg opacity-25 group-hover:opacity-40 transition duration-500"></div>
                    <div class="relative bg-gradient-to-br from-zinc-900 to-zinc-900/80 backdrop-blur-sm border border-slate-700/50 rounded-2xl p-8 md:p-10 text-center">
                        <h3 class="text-2xl md:text-3xl font-bold text-white mb-3">
                            ¿Listo para implementar esta funcionalidad?
                        </h3>
                        <p class="text-slate-300 mb-8 max-w-xl mx-auto">
                            Hablá con nuestro equipo y descubrí cómo {title.toLowerCase()} puede transformar tu práctica contable
                        </p>
                        <div class="flex flex-col sm:flex-row gap-4 justify-center">

                          <a href="/contacto"
                                class="inline-flex items-center justify-center gap-2
                                       bg-gradient-to-r from-blue-600 via-indigo-600 to-teal-400
                                       text-white px-8 py-4 rounded-xl font-bold text-lg
                                       shadow-xl shadow-blue-500/25
                                       hover:shadow-2xl hover:shadow-blue-500/40
                                       hover:scale-105 active:scale-100
                                       transition-all duration-300 group/cta"
                            >
                                Solicitar demo personalizada
                                <svg class="w-5 h-5 transition-transform group-hover/cta:translate-x-1"
                                     fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                          d="M13 7l5 5m0 0l-5 5m5-5H6"/>
                                </svg>
                            </a>
                            <a
                                href="/funciones"
                                class="inline-flex items-center justify-center gap-2
                                       bg-zinc-800/80 border border-slate-600/50
                                       text-white px-8 py-4 rounded-xl font-semibold text-lg
                                       hover:bg-zinc-700/80 hover:border-slate-500/50
                                       transition-all duration-300"
                            >
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                                </svg>
                                Ver todas las funciones
                            </a>
                        </div>
                        <p class="text-slate-400 text-sm mt-6">
                            ⚡ Implementación en 24hs • 🎯 Sin tarjeta de crédito • 💬 Soporte dedicado
                        </p>
                    </div>
                </div>
            </div>

            <!-- Related features -->
            <div class="mt-8 w-full max-w-4xl">
                <div class="bg-zinc-900/40 backdrop-blur-sm border border-slate-700/30 rounded-xl p-6">
                    <h4 class="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                        <svg class="w-5 h-5 text-teal-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                        </svg>
                        Explorá más funcionalidades
                    </h4>
                    <div class="flex flex-wrap gap-3">
                        {funciones.filter(f => f.slug !== Astro.params.slug).slice(0, 4).map(f => (
                            <a
                                href={`/funciones/${f.slug}`}
                                class="inline-flex items-center gap-2 px-4 py-2 bg-zinc-800/60 border border-slate-600/50 rounded-lg text-sm text-slate-300 hover:text-white hover:border-blue-500/50 transition-all duration-200 group"
                            >
                                <span>{f.icon}</span>
                                <span>{f.title}</span>
                                <svg class="w-4 h-4 opacity-0 -ml-2 group-hover:opacity-100 group-hover:ml-0 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                </svg>
                            </a>
                        ))}
                    </div>
                </div>
            </div>
        </section>
    </main>
</Layout>
